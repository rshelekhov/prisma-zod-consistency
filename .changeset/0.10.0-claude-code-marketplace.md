---
"prisma-zod-consistency": minor
---

**Marketing MVP #2 / D4 — Claude Code plugin marketplace.**

The Claude Code skill bundle in `packages/skill-claude-code` is now a canonical Claude Code plugin: `.claude-plugin/marketplace.json` at the repo root + `.claude-plugin/plugin.json` inside the plugin directory + SKILL.md under `skills/prisma-zod-consistency/`. Users can now install with:

```
/plugin marketplace add rshelekhov/prisma-zod-consistency
/plugin install prisma-zod-consistency
```

CLI behaviour and rule semantics are unchanged. SKILL.md was rewritten to match the 0.9.0 surface (live-DB rules R07-R09d documented as available via `--db`; "Phase 1.5 coming soon" language removed; rule references switched from broken relative paths to GitHub URLs that resolve outside the plugin install context). A README.md was added to the plugin directory for marketplace browse UI.

The Codex AGENTS.md bundle (`packages/skill-codex`) is untouched — Codex has its own distribution flow.

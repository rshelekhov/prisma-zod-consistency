{
  "name": "@prisma-zod-consistency/skill-claude-code",
  "version": "0.10.0",
  "private": true,
  "description": "Claude Code plugin for prisma-zod-consistency.",
  "license": "MIT",
  "files": [
    ".claude-plugin/plugin.json",
    "skills/prisma-zod-consistency/SKILL.md",
    "README.md"
  ],
  "scripts": {
    "build": "echo 'no build (markdown bundle)'",
    "lint": "echo 'no lint (markdown bundle)'",
    "test": "echo 'no test (markdown bundle)'",
    "typecheck": "echo 'no typecheck (markdown bundle)'"
  }
}
